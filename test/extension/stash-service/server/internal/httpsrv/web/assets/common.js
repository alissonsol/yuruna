// LICENSEURI https://yuruna.link/license
// Copyright (c) 2019-2026 by Alisson Sol et al.
//
// What the stash UI adds to the shared runtime: the addresses of one stash, the
// class icon, and the lab-token gate. Everything page-agnostic -- Y.el, Y.api,
// the chrome, Y.block, the size and date formatting -- comes from
// /assets/yuruna.core.js, which every page loads first.
//
// Untrusted stash content is ALWAYS placed via textContent / safe DOM APIs,
// never innerHTML (section 7.4).
//
// ES5 ONLY. See the header of yuruna.core.js for why, and run
// tools/Invoke-Es5Check.ps1 before shipping a change here.

// >>> yuruna-i18n embedded block -- generated by tools/Invoke-CatalogEmbed.ps1
// Edit globalization/kernel/yuruna.i18n.js or the catalog sources, then re-run
// that tool. An edit made here is overwritten and the drift gate fails first.
// Generated by tools/Invoke-CatalogCompile.ps1 from globalization/catalogs.
// Edit the source catalog and regenerate; changes made here are overwritten.
// locale: en-US  domain: stash
(function (root) {
  'use strict';
  var registry = root.YurunaCatalog = root.YurunaCatalog || {};
  var byLocale = registry['en-US'] = registry['en-US'] || {};
  byLocale['stash'] = {'stash.all':'All','stash.all_hosts':'All hosts','stash.all_types':'All types','stash.api_could_not_parse_form':'could not parse form','stash.api_create_detail':['create: ',{'arg':'detail','type':'detail','trust':'external'}],'stash.api_detail':[{'arg':'detail','type':'detail','trust':'external'}],'stash.api_invalid_host':'invalid host','stash.api_invalid_json_body':'invalid JSON body','stash.api_invalid_stash_path':'invalid stash path','stash.api_no_stashes_given':'no stashes given','stash.api_not_an_archive':'not an archive','stash.api_not_found':'not found','stash.api_nothing_to_store_provide_text_or_files':'nothing to store: provide text or files','stash.api_open_archive_detail':['open archive: ',{'arg':'detail','type':'detail','trust':'external'}],'stash.api_open_artifact':'open artifact','stash.api_open_upload':'open upload','stash.api_open_uploads':'open uploads','stash.api_search_detail':['search: ',{'arg':'detail','type':'detail','trust':'external'}],'stash.api_stash_not_found':'stash not found','stash.api_stat_artifact':'stat artifact','stash.api_too_many_files_in_one_upload':'too many files in one upload','stash.api_too_many_stashes_in_one_request':'too many stashes in one request','stash.archive':'Archive','stash.archive_contents':'Archive contents','stash.archive_value1_contents':['Archive (',{'arg':'value1','type':'detail','trust':'external'},') -- contents:'],'stash.audio':'Audio','stash.author_optional':'Author (optional)','stash.choose_at_least_one_file':'Choose at least one file.','stash.class':'class','stash.content':'Content','stash.could_not_be_checked':'could not be checked','stash.could_not_list_archive_value1':['Could not list archive: ',{'arg':'value1','type':'detail','trust':'external'}],'stash.could_not_load_text_http_value1':['Could not load text: HTTP ',{'arg':'value1','type':'detail','trust':'external'}],'stash.could_not_load_text_value1':['Could not load text: ',{'arg':'value1','type':'detail','trust':'external'}],'stash.create_failed_value1':['Create failed: ',{'arg':'value1','type':'detail','trust':'external'}],'stash.create_stash':'Create stash','stash.created':'Created','stash.delete':'Delete','stash.delete_failed_for_value1_value2':['Delete failed for ',{'arg':'value1','type':'detail','trust':'external'},': ',{'arg':'value2','type':'detail','trust':'external'}],'stash.delete_failed_value1':['Delete failed: ',{'arg':'value1','type':'detail','trust':'external'}],'stash.delete_is_locked_unlock_actions_with_the_lab_token_above_or_open_':'Delete is locked. Unlock actions with the Lab token above, or open this page from the Yuruna hosts dashboard, which unlocks it for you.','stash.delete_is_unavailable_this_service_has_no_pool_aggregator_configu':'Delete is unavailable: this service has no pool aggregator configured, so no Lab token or dashboard link can be checked.','stash.delete_is_unavailable_this_service_has_no_pool_aggregator_configu_9d79c432':' Delete is unavailable: this service has no pool aggregator configured, so no Lab token can be checked.','stash.delete_selected':'Delete selected','stash.delete_stash_value1':['Delete stash ',{'arg':'value1','type':'detail','trust':'external'}],'stash.delete_stash_value1_this_cannot_be_undone':['Delete stash ',{'arg':'value1','type':'detail','trust':'external'},'? This cannot be undone.'],'stash.delete_stash_value1_value2_value3_this_cannot_be_undone':['Delete stash ',{'arg':'value1','type':'detail','trust':'external'},' (',{'arg':'value2','type':'detail','trust':'external'},', ',{'arg':'value3','type':'detail','trust':'external'},')? This cannot be undone.'],'stash.delete_value1_this_cannot_be_undone':['Delete ',{'arg':'value1','type':'detail','trust':'external'},'? This cannot be undone.'],'stash.deleting':'Deleting...','stash.dir':'dir','stash.download':'Download','stash.error_value1':['Error: ',{'arg':'value1','type':'detail','trust':'external'}],'stash.file_s_multiple_files_are_grouped_into_one_zip':'File(s) -- multiple files are grouped into one ZIP','stash.filter_by_content_type':'Filter by content type','stash.filter_by_host':'Filter by host','stash.guide':'Guide','stash.host':'Host','stash.host_4740ae63':'host','stash.if_unlocking_answers_that_the_code_could_not_be_checked_the_code_':'If unlocking answers that the code could not be checked, the code is not the problem -- the pool aggregator that checks it is unreachable, and you cannot unlock until it is back.','stash.image':'Image','stash.incomplete_upload_partial_bytes_available_via_download':'Incomplete upload -- partial bytes available via Download.','stash.ips':'IPs:','stash.lab_token':'Lab token','stash.limit':'limit','stash.listed_count':{'kind':'plural','selector':'count','variants':{'one':[{'arg':'count','type':'integer','trust':'internal'},' stash (showing ',{'arg':'shown','type':'integer','trust':'internal'},')'],'other':[{'arg':'count','type':'integer','trust':'internal'},' stashes (showing ',{'arg':'shown','type':'integer','trust':'internal'},')']}},'stash.load_more':'Load more','stash.loaded':'Loaded:','stash.loading':'Loading...','stash.local_host_name':[{'arg':'host','type':'detail','trust':'external'},'  (this host)'],'stash.locked_unlock_actions_with_the_lab_token_above_or_open_this_page_':' Locked -- unlock actions with the Lab token above, or open this page from the Yuruna hosts dashboard.','stash.menu':'Menu','stash.name':'Name','stash.new_stash':'New stash','stash.new_stash_yuruna_stash':'New stash \u2014 Yuruna Stash','stash.no_inline_preview_for_this_type_download_to_view':'No inline preview for this type (download to view).','stash.no_pool_aggregator_is_configured_so_no_lab_token_can_be_checked_a':['No pool aggregator is configured, so no Lab token can be checked and nothing here can be deleted. Set ',{'arg':'command1','type':'token','trust':'internal'},' on the daemon to enable it.'],'stash.notes_txt':'notes.txt','stash.nothing_to_store_paste_some_content_first':'Nothing to store -- paste some content first.','stash.offset':'offset','stash.open_on_that_host':'open on that host','stash.open_pdf':'Open PDF','stash.other':'Other','stash.pages':'Pages','stash.paste_content_here':'Paste content here...','stash.paste_text':'Paste text','stash.pause':'Pause','stash.pdf':'PDF','stash.permalink':'Permalink','stash.preview_truncated_download_for_the_full_content':'Preview truncated -- Download for the full content.','stash.preview_unavailable_use_download_to_open_the_file':'Preview unavailable. Use Download to open the file.','stash.q':'q','stash.received':'Received','stash.received_by_host':' Received by host ','stash.received_host':['Received by host ',{'arg':'host','type':'detail','trust':'external'}],'stash.refresh':'Refresh','stash.rescan_the_stash_share_for_other_hosts_stashes':'Rescan the stash share for other hosts\' stashes','stash.scp_path':'SCP path','stash.search_id_filename_user_path':'Search id, filename, user, path...','stash.select_stash_value1':['Select stash ',{'arg':'value1','type':'detail','trust':'external'}],'stash.selection_count':{'kind':'plural','selector':'count','variants':{'one':[{'arg':'count','type':'integer','trust':'internal'},' stash'],'other':[{'arg':'count','type':'integer','trust':'internal'},' stashes']}},'stash.short_link':'Short link','stash.six_character_lab_token_from_the_dashboard':'Six-character lab token from the dashboard','stash.size':'Size','stash.sort':'sort','stash.source':'Source','stash.stash':'Stash','stash.stash_not_found':'Stash not found.','stash.stash_text_preview':'Stash text preview','stash.stash_yuruna_stash':'Stash \u2014 Yuruna Stash','stash.stashes':'Stashes','stash.stashes_yuruna_stash':'Stashes \u2014 Yuruna Stash','stash.state_archive':'archive','stash.state_audio':'audio','stash.state_complete':'complete','stash.state_image':'image','stash.state_other':'other','stash.state_partial':'partial','stash.state_pdf':'pdf','stash.state_pending':'pending','stash.state_text':'text','stash.state_truncated':'truncated','stash.state_video':'video','stash.status':'Status','stash.still_receiving_no_preview_yet':'Still receiving -- no preview yet.','stash.text':'Text','stash.the_6_character_code_on_the_yuruna_hosts_dashboard_s_lab_token_ti':'The 6-character code on the Yuruna hosts dashboard\'s Lab token tile; upper or lower case both work. Opening this page from that dashboard unlocks it without the code. Needed only to delete.','stash.this_host':'this host','stash.title_filename_optional':'Title / filename (optional)','stash.truncated_at_the_100_mb_cap_download_serves_the_capped_artifact':'Truncated at the 100 MB cap -- Download serves the capped artifact.','stash.type':'Type','stash.unlock_actions':'Unlock actions','stash.unlock_actions_to_delete':'Unlock actions to delete','stash.unnamed':'(unnamed)','stash.upload_file_s':'Upload file(s)','stash.upload_stash':'Upload stash','stash.user':'User','stash.value1_malformed_permalink':[{'arg':'value1','type':'detail','trust':'external'},' (malformed permalink)'],'stash.value1_of_value2_could_not_be_deleted_value3':[{'arg':'value1','type':'detail','trust':'external'},' of ',{'arg':'value2','type':'detail','trust':'external'},' could not be deleted -- ',{'arg':'value3','type':'detail','trust':'external'}],'stash.value1_pdf_preview':[{'arg':'value1','type':'detail','trust':'external'},' (PDF preview)'],'stash.value1_yuruna_stash':[{'arg':'value1','type':'detail','trust':'external'},' -- Yuruna Stash'],'stash.video':'Video','stash.web':'web','stash.yuruna_stash':'Yuruna Stash','stash.yuruna_stash_service':'Yuruna stash service'};
}(this));
// <<< yuruna-i18n embedded block
(function (window, document) {
  'use strict';

  var Y = window.Y;

  // Guards the one-time submit wiring in Y.initUnlock below.
  var unlockWired = false;

  // pathTail derives /<yyyy>/<mm>/<dd>/<id> from a view's permalink, which is
  // the authoritative /s/<host>/<yyyy>/<mm>/<dd>/<id> the server built.
  //
  // A malformed view (missing or non-string permalink) returns null instead of
  // throwing, so one bad row cannot crash the render of every other row.
  function pathTail(view) {
    if (!view || typeof view.permalink !== 'string') { return null; }
    var parts = view.permalink.split('/').filter(Boolean); // [s, host, y, m, d, id]
    return '/' + parts.slice(2).join('/');
  }

  // Propagate pathTail's null so a bad permalink yields no link: Y.el skips a
  // null href/src attribute rather than building a broken URL from it.
  Y.rawURL = function (view) {
    var tail = pathTail(view);
    return tail === null ? null : '/raw/' + view.hostId + tail;
  };
  Y.downloadURL = function (view) {
    var tail = pathTail(view);
    return tail === null ? null : '/download/' + view.hostId + tail;
  };

  // The REST endpoint for one listed stash (GET / DELETE). Same null
  // propagation: a malformed permalink yields no URL at all rather than a
  // DELETE aimed at a guessed path -- the caller must not destroy a stash it
  // could not address.
  Y.stashApiURL = function (view) {
    var tail = pathTail(view);
    return tail === null ? null : '/api/stashes/' + view.hostId + tail;
  };

  // The same stash as the bulk-delete API's field form. Null for a malformed
  // permalink, for the same reason stashApiURL is: a stash that cannot be
  // addressed exactly must not be named in a request that destroys things.
  Y.stashKey = function (view) {
    var tail = pathTail(view);
    if (tail === null) { return null; }
    var parts = tail.split('/').filter(Boolean); // [y, m, d, id]
    if (parts.length !== 4) { return null; }
    return { hostId: view.hostId, year: parts[0], month: parts[1], day: parts[2], id: parts[3] };
  };

  // Surrogate pairs, for two separate reasons. The \u{...} form these were
  // written in is ES2015: the baseline parser rejects it outright, and a parse
  // error here takes the whole file with it rather than just the icon. And the
  // pairs keep this source ASCII, so the bytes survive any tool in the path
  // that is not told the encoding.
  Y.classIcon = function (cls) {
    switch (cls) {
      case 'text': return '\uD83D\uDCC4';      // page
      case 'image': return '\uD83D\uDDBC';     // framed picture
      case 'pdf': return '\uD83D\uDCD5';       // closed book
      case 'audio': return '\uD83D\uDD0A';     // speaker
      case 'video': return '\uD83C\uDFAC';     // clapper
      case 'archive': return '\uD83D\uDCE6';   // package
      default: return '\uD83D\uDCBE';          // floppy
    }
  };

  // The one attempt to spend a control proof carried in from the Yuruna hosts
  // dashboard, started at load so the gate is already open by the time a page
  // reads it. Arriving through the dashboard's Extension hosts link is then
  // enough to delete -- the operator is not sent back to copy the rotating code
  // off a tile.
  //
  // Decoded, unlike the other services: this service's redirect percent-encodes
  // the fragment it hands over.
  Y.startProofUnlock({ decode: true });

  // Y.session reports what this browser may do. The proof is awaited rather
  // than raced: reading the gate first would render the lab-token prompt for a
  // device that was about to be unlocked anyway, and the prompt would then be
  // answered by an operator who never needed to see it. A failed read reports a
  // locked, unconfigured gate -- a page must offer no control it cannot vouch
  // for.
  Y.session = function () {
    return Y.proofUnlock.then(function () {
      return Y.api('/api/session');
    }).then(function (s) {
      return { authed: !!s.authed, labToken: !!s.labToken, configured: !!s.configured };
    }, function () {
      return { authed: false, labToken: false, configured: false };
    });
  };

  // Y.initUnlock wires the shared lab-token prompt: it shows the form only when
  // the gate is on and this device is not through it, names the case where no
  // gate is configured at all, and re-runs the page's own render after a
  // successful unlock so the controls appear without a reload. Resolves with
  // the session it read, so a caller gets the answer and the wiring from one
  // call.
  Y.initUnlock = function (onUnlocked) {
    return Y.session().then(function (sess) {
      var login = document.getElementById('login');
      var unconfigured = document.getElementById('gate-unconfigured');
      if (login) { login.hidden = !(sess.labToken && !sess.authed); }
      if (unconfigured) { unconfigured.hidden = sess.configured; }
      // The form is wired once for the page's life: initUnlock is called again
      // after every unlock and every reload, and a second listener on the same
      // form would submit the code twice.
      var form = document.getElementById('login-form');
      if (form && !unlockWired) {
        unlockWired = true;
        form.addEventListener('submit', function (ev) {
          ev.preventDefault();
          var field = document.getElementById('lab-token');
          var err = document.getElementById('login-error');
          if (err) { err.textContent = ''; }
          // Normalized here as well as at the daemon, so a code read off the
          // tile in capitals is not a round trip that comes back "incorrect".
          Y.api('/api/login', { method: 'POST', body: { labToken: field.value.trim().toLowerCase() } })
            .then(function () {
              field.value = '';
              if (typeof onUnlocked === 'function') { return onUnlocked(); }
              return null;
            }, function (e) {
              if (err) { err.textContent = e.message; }
            });
        });
      }
      return sess;
    });
  };
}(window, document));

(function (window) {
  'use strict';
  var known = {"pending": true, "partial": true, "complete": true, "truncated": true, "text": true, "image": true, "pdf": true, "audio": true, "video": true, "archive": true, "other": true};
  window.Y.displayState = function (value) {
    return Object.prototype.hasOwnProperty.call(known, value)
      ? window.YurunaI18n.t("stash.state_" + value) : value;
  };
}(window));
