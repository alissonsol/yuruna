{
  "$schema": "Interaction sequence for driving VM installation.",
  "$actions": {
    "delay":            "Wait a fixed number of seconds. Parameters: seconds (number).",
    "key":              "Send a single keystroke. Parameters: name (string). Supported: Enter, Tab, Space, Escape, Up, Down, Left, Right, F1-F12.",
    "type":             "Type a text string. Parameters: text (string), sensitive (boolean, masks output), charDelayMs (number, delay between chars in ms, default 50).",
    "typeAndEnter":     "Type a text string, wait, then press Enter. Parameters: text (string), sensitive (boolean, masks output), charDelayMs (number, default 50), delaySeconds (number, default 2).",
    "screenshot":       "Capture a screenshot for debugging. Parameters: label (string, used in filename).",
    "waitForText":      "Capture + OCR the VM screen until pattern appears. Parameters: pattern (string), timeoutSeconds (number), pollSeconds (number), freshMatch (boolean, waits for pattern to clear first if already on screen). Uses ${var} substitution.",
    "waitForPort":      "Wait until a TCP port responds on the VM. Parameters: port (number), timeoutSeconds (number).",
    "waitForHeartbeat": "Wait for Hyper-V Integration Services heartbeat. Parameters: timeoutSeconds (number). Hyper-V only.",
    "waitForVMStop":    "Wait until the VM reaches the Off/stopped state. Parameters: timeoutSeconds (number)."
  },
  "$variables": "Define defaults below. ${vmName}, ${hostType}, ${guestKey} are built-in. Use ${varName} in step parameters.",

  "description": "Ubuntu Desktop: dismiss GRUB 'Try or Install' menu then let autoinstall run unattended.",
  "variables": {
    "username":    "ubuntu",
    "password":    "password",
    "newPassword": "belohorizonte"
  },
  "steps": [
    {
      "action": "waitForText",
      "pattern": "Try or Install Ubuntu",
      "timeoutSeconds": 180,
      "pollSeconds": 5,
      "description": "Wait for login prompt"
    },
    {
      "action": "delay",
      "seconds": 1,
      "description": "Brief pause before GRUB dismissal"
    },
    {
      "action": "typeAndEnter",
      "text": "",
      "description": "Dismiss GRUB menu"
    },
    {
      "action": "waitForText",
      "pattern": "Not listed?",
      "timeoutSeconds": 900,
      "pollSeconds": 15,
      "description": "Login after install"
    },
    {
      "action": "typeAndEnter",
      "text": "",
      "description": "Bring up password field"
    },
    {
      "action": "waitForText",
      "pattern": "Password",
      "timeoutSeconds": 30,
      "pollSeconds": 1,
      "description": "Wait for password prompt"
    },
    {
      "action": "typeAndEnter",
      "text": "${password}",
      "sensitive": true,
      "description": "Enter current password"
    },
    {
      "action": "waitForText",
      "pattern": "Current password",
      "timeoutSeconds": 30,
      "pollSeconds": 1,
      "description": "Confirm password before change"
    },
    {
      "action": "typeAndEnter",
      "text": "${password}",
      "sensitive": true,
      "description": "Confirm current password"
    },
    {
      "action": "waitForText",
      "pattern": "New password",
      "timeoutSeconds": 60,
      "pollSeconds": 2,
      "description": "Wait for password prompt"
    },
    {
      "action": "typeAndEnter",
      "text": "${newPassword}",
      "sensitive": true,
      "description": "The new password: ${newPassword}"
    },
    {
      "action": "waitForText",
      "pattern": "Retype new password",
      "timeoutSeconds": 30,
      "pollSeconds": 1,
      "description": "Wait for password prompt"
    },
    {
      "action": "typeAndEnter",
      "text": "${newPassword}",
      "sensitive": true,
      "description": "The new password: ${newPassword}"
    },
    {
      "action": "waitForText",
      "pattern": "${username}@${vmName}",
      "timeoutSeconds": 30,
      "pollSeconds": 1,
      "description": "Wait for login shell prompt"
    },
    {
      "action": "typeAndEnter",
      "text": "sudo bash /automation/fetch-and-execute.sh vde/guest.ubuntu.desktop/ubuntu.desktop.update.sh",
      "description": "Run update script"
    },
    {
      "action": "waitForText",
      "pattern": "[sudo] password for ${username}",
      "timeoutSeconds": 30,
      "pollSeconds": 1,
      "description": "Wait for password prompt"
    },
    {
      "action": "typeAndEnter",
      "text": "${newPassword}",
      "sensitive": true,
      "description": "New password"
    },
    {
      "action": "waitForText",
      "pattern": "${username}@${vmName}",
      "timeoutSeconds": 600,
      "pollSeconds": 15,
      "freshMatch": true,
      "description": "Wait for update script to complete (may take several minutes)"
    },
    {
      "action": "delay",
      "seconds": 1,
      "description": "Brief pause before rebooting"
    },
    {
      "action": "typeAndEnter",
      "text": "sudo reboot now",
      "description": "Reboot the VM, for the fun of it!"
    }
  ]
}
